apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  template:
    spec:
      containers:
        - name: frontend
          env:
            - name: FASTAPI_BASE_URL
              value: "http://backend.rag-prod.svc.cluster.local:8000"
